version: 2

sources:
  - name: source
    database: "{{ target.database }}"
    schema: source
    tables:
      - name: fct_sales

      - name: fct_returns

      - name: dim_dates

      - name: dim_customers

      - name: dim_products

      - name: dim_stores

      - name: items
